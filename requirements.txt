# --- Web framework (Phase 0) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9   # needed if you later accept file uploads (e.g. custom datasets)

# --- Data handling (Phase 1+) ---
pandas==2.2.2
numpy==1.26.4

# --- Machine Learning (Phase 2+) ---
scikit-learn==1.5.1
xgboost==2.1.1

# --- Database (Phase 2+, mission/score storage) ---
# SQLite itself needs no package (built into Python's standard library),
# but SQLAlchemy makes querying/inserting much cleaner than raw SQL.
sqlalchemy==2.0.32

# --- Testing (Phase 7, but good to install early) ---
pytest==8.3.2
httpx==0.27.2   # required by FastAPI's TestClient for API tests